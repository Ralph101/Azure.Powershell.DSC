@{
    AllNodes = @(
        @{
            NodeName    = 'ITSAZSWDOM01.rdshOnAzure.local'
            Role        = 'DC'
            PSDscAllowPlainTextPassword = $true
            }
        )
    }
